/** Shared event constants, safe to import from client components — no API
 *  keys or server-only code here. */

/** Public Luma event page. The CTAs that go straight to Luma point here, and
 *  the server helpers fall back to it when the API can't be reached. */
export const EVENT_URL = "https://luma.com/overhang26";
